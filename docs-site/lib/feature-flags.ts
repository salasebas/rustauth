/**
 * In-docs AI chat (sidebar panel + floating "Ask AI" trigger).
 * Logic lives in `components/ai-chat.tsx` and `lib/ai-chat/`; flip to `true` to re-enable.
 */
export const DOCS_AI_CHAT_ENABLED = false;
